<Window x:Class="InterviewTestApp.Windows.CustomerCreateEditView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:InterviewTestApp.Windows"
        mc:Ignorable="d"
        Title="CustomerCreateEditView" 
        WindowStartupLocation="CenterOwner"
        SizeToContent="WidthAndHeight"
        ResizeMode="NoResize">
    <Window.Resources>

        <Style TargetType="TextBox">
            <Setter Property="Validation.ErrorTemplate">
                <Setter.Value>
                    <ControlTemplate>
                        <DockPanel>
                            <Border BorderBrush="Red" BorderThickness="1">
                                <AdornedElementPlaceholder/>
                            </Border>
                        </DockPanel>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

            <Style.Triggers>
                <Trigger Property="Validation.HasError" Value="True">
                    <Setter Property="ToolTip"
                            Value="{Binding (Validation.Errors)[0].ErrorContent,
                                            RelativeSource={RelativeSource Self}}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="250"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Margin="0 0 8 8"
                   VerticalAlignment="Center" Text="Name:"/>
        <TextBox   Grid.Row="0" Grid.Column="1" Margin="0 0 0 8"
                   Text="{Binding Name, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay,ValidatesOnNotifyDataErrors=True}"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Margin="0 0 8 8"
                   VerticalAlignment="Center" Text="Email:"/>
        <TextBox   Grid.Row="1" Grid.Column="1" Margin="0 0 0 8"
                   Text="{Binding Email, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay,ValidatesOnNotifyDataErrors=True}"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Margin="0 0 8 8"
                   VerticalAlignment="Center" Text="Phone:"/>
        <TextBox   Grid.Row="2" Grid.Column="1" Margin="0 0 0 8"
                   Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay,ValidatesOnNotifyDataErrors=True}"/>

        <TextBlock Grid.Row="3" Grid.Column="0" Margin="0 0 8 8"
                   VerticalAlignment="Center" Text="ActiveState:"/>
        <ComboBox Grid.Row="3" Grid.Column="1" Margin="0 0 0 8"
                  ItemsSource="{Binding ActiveStateValues}"
                  SelectedItem="{Binding ActiveState, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

        <StackPanel Grid.Row="4" Grid.ColumnSpan="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="Cancel"
                    Width="80"
                    Margin="0 8 8 0"
                    Command="{Binding CancelCommand}"/>
            <Button Content="Save"
                    Width="80"
                    Margin="0 8 0 0"
                    Command="{Binding SaveCommand}"/>
        </StackPanel>
    </Grid>
</Window>
